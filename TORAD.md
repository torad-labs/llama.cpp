# torad-labs/llama.cpp

`main` is PrismML's llama.cpp at tag `prism-b10685-7dffb15` (itself ggml-org/llama.cpp b10685
plus the ternary PQ/PTQ kernels) with the commits below on top. Each is one self-contained change
with its own measurement and its own off switch, so any of them can be rebased, dropped or sent
upstream on its own. Nothing here is model-specific: the examples are Ternary Bonsai 2 27B because
that is the model these were measured on. [docs/torad/bonsai-27b](docs/torad/bonsai-27b/README.md)
has that model against PrismML's latest release, with every command to reproduce it and to serve
it to Claude Code.

This repo carries kernels and server features only. How a build is produced per GPU, how a model
is fetched, derived, gated and served, lives in [torad-labs/rig](https://github.com/torad-labs/rig),
which pins a commit of this branch as a submodule.

| commit | change | off switch |
|---|---|---|
| in `60feea0` | `--checkpoint-every N`: a pinned context checkpoint every N prompt tokens and one at the exact token a prompt forked, so a compaction or a new session on a recurrent/hybrid model resumes from a checkpoint instead of re-prefilling from zero | omit the flag |
| `60feea0` | tensor-core flash attention reads a q4_0 K/V cache natively (head size 256, GQA > 4) with int8 Q·K; decode uses it from 4,096 tokens on. RTX 5080: prefill at 131K 930 → 1,382 tok/s, decode at 131K 44.5 → 73.3 tok/s | `GGML_CUDA_FATTN_Q4_0_LEGACY=1` |
| `96aa8e2` | a rank-1 LoRA fused into one decode launch per adapted weight (dot + scaled outer add) instead of four; `test-backend-ops LORA_RANK1` checks the graph against CPU | `GGML_CUDA_LORA_RANK1_FUSE=0` |
| `55b7db6` | a small float matrix mmf cannot tile (rows not a multiple of 32) takes the vector kernel instead of cuBLAS's ~26 µs 4-block GEMM while rows × cols ≤ 512: the 48-row `ssm_alpha`/`ssm_beta` of every Gated DeltaNet layer at each MTP verify. RTX 5070 Ti, served head with MTP: 83.3 → 90.1 / 90.8 tok/s (A-B-A) | `GGML_CUDA_MMVF_UNTILED_LEGACY=1` |
| `b120f63` | chunked tensor-core Gated DeltaNet prefill, a port of ggml-org/llama.cpp#26001 (16-token chunks: f32 forward substitution, fp16 WMMA Q·Kᵀ and state/output with f32 accumulation, f32 state); reads qwen35's q/k/v views and raw gates in place and leaves the last K-1 tokens of a ubatch on the recurrent kernel, so MTP rollback snapshots are unchanged. RTX 5070 Ti: GDN per 512-token ubatch 53.3 → 15.6 ms, prefill pp512 at 32,768 1,330 → 1,446 tok/s; KL against the recurrent kernel 0.00148 (the int8 Q·K bar: 0.00150) | `GGML_CUDA_GDN_CHUNKED=0` |
| `d371dff` | GeForce Blackwell (sm_120) takes two paths that were gated to the DGX Spark: the transposed dim-0 concat for a Gated DeltaNet layer's conv state (3 columns) with the new rows, 11.50 → 1.77 µs per layer at an MTP verify, and the float4 scale, 6.10 → 5.14 µs. RTX 5070 Ti, 3-row verify step: GPU time 20.0 → 19.4 ms (−2.8%), logits identical (KL at the protocol's noise floor, same top p 100 %) | `GGML_CUDA_CONCAT_TRANSPOSE_SM120_LEGACY=1`, `GGML_CUDA_SCALE_VEC4_SM120_LEGACY=1` |
| `9e02027` | a q8_0 recurrent-state cache (`-cts q8_0`) takes the fused snapshot write: the Gated DeltaNet kernel quantizes each warp-wide slice of a state column to one q8_0 block as it stores it (cpy's formula, so the cache holds the same bytes) and the per-layer f32 → q8_0 cpy (~14 µs) is skipped at decode and MTP verify; the chunked prefill keeps its cpy. RTX 5070 Ti, GPU time per step with one state written: decode 14.15 → 13.40 ms (−5.3 %), a 3-token ubatch 16.94 → 16.57 ms; served with the MTP draft head, where a verify writes K = 3 snapshots per layer (three cpys before this), legacy / fused A-B-A-B 104.8 / 118.1 / 110.1 / 120.2 tok/s (+9-10 %) with the same draft acceptance and the same text byte for byte; logits identical (KL at the protocol's noise floor, same top p 100 %) | `GGML_CUDA_GDN_Q8_CACHE_LEGACY=1` |
| `cf29278` | a fused gate/up + GLU on mul_mat_vec_q may write its output over src1, which that kernel reads only through the q8_1 copy it quantizes first: at decode ggml-alloc puts the GLU output at the FFN input's address on 24 of Ternary Bonsai 2 27B's 64 layers (three in every eight), and the buffer-overlap check refused the fusion there; mul_mat_vec_f reads src1 in the kernel that writes the output and is still refused. RTX 5070 Ti, decode: 40 → 64 of 64 layers fused, 72 fewer launches per step; a refused layer cost 70.1 µs (two 17,408-row matvecs, a quantize, the GLU) against 65.0 µs fused, about 120 µs of a 13.8 ms step (Nsight, within one run); the served MTP head is unchanged, 112.5 / 112.6 / 112.6 tok/s (A-B-A) with the same text byte for byte, as its verify rows never take this path; logits identical (KL at the protocol's noise floor, same top p 100 %) | `GGML_CUDA_MMVQ_FUSION_SRC1_LEGACY=1` |
| `f94fe8a` | a lazy grammar's WORD triggers (regex-escaped literals: `<tool_call>`, one `<function=NAME>` per tool) are searched only where the bytes a token appended can complete them, instead of every trigger regex over every generated byte at every token, which is quadratic over a turn; real regexes (anchored, classes, alternations) keep the whole-buffer search. 24 tools, plain content, per accepted token: 0.14 → 0.000 ms at 512 tokens, 2.50 → 0.000 ms at 4,096 (vocab-only harness, the served chat template); a trigger in the text fires on the same token either way and the grammar accepts the same 5,568 tokens | `LLAMA_GRAMMAR_TRIGGER_LITERAL_LEGACY=1` |
| `d07e5b8` | a CUDA graph is found by the first node's address, the node count and the first and last nodes' shapes, not the address alone: the MTP draft context builds its catch-up batch (the verify rows) and its one-row draft step in turn from one first node, so they shared one CUDA graph and each found the other's properties there, and every round ran the catch-up and the first draft step uncaptured (89 kernel launches) and captured the second step again. Ternary Bonsai 2 27B, served flags, MTP n_max 2, RTX PRO 6000: uncaptured launches per round 89 → 0 (nsys, 211 rounds at 127K); ms per round at 13.8K / 61.8K / 127K context 11.67 / 13.00 / 15.29 → 11.38 / 12.56 / 14.81 (−2.5 / −3.4 / −3.1 %), with `-bs` 11.14 / 12.41 / 14.57 → 11.10 / 12.34 / 14.48; 48 greedy eval requests: text identical 48/48, same draft counters, +1.1 % tok/s | `GGML_CUDA_GRAPH_KEY_LEGACY=1` |
| `99c3f63` | backend sampling (`-bs`) stays on for a request with a lazy grammar or a reasoning budget (a tool request has both) while neither would change the next token's logits: a grammar awaiting its trigger and a budget that is not forcing are passive. The slot leaves the backend before the decode that samples a constrained token, and right after a speculative verification (before a checkpoint restore rewinds the sampler); a verification ends on the token that switched a constraint on; a sampler the context stops running is released back to the CPU (the chain kept `is_init` and selected no token). A `--pull-layers` server keeps its slots off `-bs` (`02efc73`): the detector reads the served logits before the sampler. RTX PRO 6000, Ternary Bonsai 2 27B, MTP n_max 2, 48 greedy eval requests (24 with tools), with `3ab698f`, `-bs` against off: agentic +1.2 % tok/s (these ran the CPU sampling path before this change), chat / code / math +2.2 / +2.6 / +2.6 % (on the backend already), all +2.1 % (run-to-run noise −0.07 %); 47 of 48 texts identical, the other parting at a 0.003-nat near-tie of the plain model; top-1 logprob legs 48 of 48 identical | `LLAMA_SAMPLING_BACKEND_PASSIVE_LEGACY=1` |
| `3ab698f` | a backend sampler leaving the graph (`llama_set_sampler(nullptr)`) no longer re-reserves the scheduler (~18 ms on the 27B hybrid, a stall of every slot): the buffers were reserved with that sampler's nodes counted and the graph without it fits them. A slot whose lazy grammar triggers leaves mid-generation, so every tool request paid it, and so did every request's end. Same 48 requests with `-bs`: 86 → 51 reserves (3 without `-bs`); against `-bs` off, agentic median −4.8 → −0.6 %, aggregate −0.5 → +1.2 % | `LLAMA_SAMPLER_DETACH_RESERVE_LEGACY=1` |
| `383771d` | a backend sampler attaching to a context reserves the scheduler only when the graph with it exceeds the node budget of the last reserve (`graph_max_nodes`); a compute buffer the graph outgrows is still reallocated when the scheduler allocates it. The server attaches a slot's chain at every request launch, so a same-shaped chain re-entering a graph already reserved for it paid ~18 ms, a stall of every slot. Same 48 requests with `-bs` on an RTX PRO 6000: 51 → 3 reserves (the count without `-bs`), reserve time 966 → 55 ms, about 19 ms less to the first token of each request (total request time 220.1 → 219.1 s, −0.45 %), decode tok/s unchanged (+0.1 %, inside the run-to-run noise), 18 releases, 48 of 48 texts identical to `3ab698f`'s; `test-backend-sampler --test reattach_no_reserve` counts 0 reserves on a reattach, 1 with the switch set | `LLAMA_SAMPLER_ATTACH_RESERVE_LEGACY=1` |
| `fa69d7a` | the Gated DeltaNet output projection takes its input as 2D rows ([6144, n_tokens]) instead of 3D with a reshape back after it, so the layer's residual ADD follows the MUL_MAT and fuses into mul_mat_vec_q like the attention and FFN ones. RTX 5070 Ti, decode (ubatch 1), Nsight: the 48 GDN ADDs fused, k_bin_bcast 49 → 1 launch per step, −48.9 µs of add kernels against +0.2 µs per fused projection, the step inside run-to-run noise; at 2-4 columns the ADD fuses once mul_mat_vec_q fuses there. Logits identical (KL at the protocol's noise floor at ubatch 1, 3, 512 and four sequences, same top p 100 %), served text identical | `LLAMA_GDN_OUT_PROJ_3D_LEGACY=1` |
| `0b631d6` | a MUL_MAT fuses its bias or residual ADD into mul_mat_vec_q at up to 3 columns instead of 1, so at an MTP verify with two drafts (2-3 rows) the ADDs after the attention output, the FFN down and the Gated DeltaNet output projection no longer run as their own k_bin_bcast; a gate/up + GLU still fuses at one column only (two rows of both matrices took 101-134 registers per thread against 64-89 unfused and ran 12-19 % slower than the two kernels it replaced). RTX 5070 Ti, served flags, Nsight per step at 2 / 3 rows: k_bin_bcast 129 → 1 launch, 16.91 → 16.69 / 16.67 → 16.56 ms (−1.3 / −0.6 %), 4 rows unchanged (nothing fuses there); served with the MTP draft head, 48 greedy eval requests A-B-B-A: +1.4 ± 1.3 % tok/s per request, text identical 48/48, same draft acceptance; perplexity identical at 2, 3 and 4 rows | `GGML_CUDA_MMVQ_FUSION_MULTICOL_LEGACY=1` |
| `cbe42cc` | with `--kv-unified` a decoding slot's mask has the other slots' cells masked before or after its own, and flash attention read and discarded them tile by tile; for at most 16 Q rows on at least 4,096 cells the mask pre-pass now finds each Q tile's first and last live FATTN_KQ_STRIDE tile (one tile per thread, 16-byte mask reads) and the mma kernel's stream-k blocks run only between them (upstream's pre-pass found only the upper bound, from 1,024 rows; Johannes Gaessler's suggestion in ggml-org#13493). RTX 5070 Ti, the served head at 2 slots × 131,072, MTP n_max 2, slot 1 decoding while slot 0 holds ~130k tokens, greedy, 4 prompts × 2 rounds: 84.6 → 95.2 tok/s (+12.6 %), the penalty against an empty pool −17.1 → −10.3 %; with nothing to skip (one sequence at 32,768 / 65,536) ~4.2 µs of pre-pass per attention layer, tok/s inside the run-to-run spread; `test-backend-ops FLASH_ATTN_EXT` 3068/3068 both ways, 116 of them banded-mask cases | `GGML_CUDA_FATTN_KV_RANGE_LEGACY=1` |

Every switch in the last column is read once per process and parses as an integer: a `*_LEGACY` switch set to `0`
is the same as unset (the change stays on), and `=0` turns off `GGML_CUDA_LORA_RANK1_FUSE` and
`GGML_CUDA_GDN_CHUNKED`.

Remotes for maintenance: `prism` → PrismML-Eng/llama.cpp (the base), merged in when wanted. The
83 PrismML branches this repo was first pushed with were removed on 2026-09-20 — every one was at
the same commit as PrismML's copy; they are one `git fetch prism` away.

## CI in this repo

Upstream's `.github/workflows/` (52 workflows) is deleted on `main`: this fork does not run
upstream's build matrix, and 52 third-party workflows are a supply-chain surface nobody here
audits. The org's required checks (secret scan, workflow lint, PR title) run from the org, not
from files here. When a merge from `prism` brings workflows back, delete them again in the
merge commit. Builds of this fork are made and verified by rig, per GPU.

`.gitleaks.toml` narrows three false-positive classes found in the full 10,688-commit upstream
history (Actions cache-key names, xxhash's `data_key_lo/hi`, a README placeholder), each opened
at its commit on 2026-09-20 and none live. Proven on gitleaks v8.21.2 with canaries: control 15
(12 findings + 3 canaries, no config), red 3 of 3 canaries still firing with the config — two of
them on the same line as an allowlisted value — and green 0 on `main`.
